# Hardware tests

> **Status:** stub — content to be written.

**Audience:** contributors with a real rig (or a piece of it).
**Scope:** the ``hardware`` marker: per-family smoke tests, opt-in env var, conventions.

## Will cover

- ``CAPA_HARDWARE_TESTS=1`` opt-in
- Per-family smoke tests under ``tests/hardware/``
- Conventions for hardware fixtures (no leaks across tests)
- When a hardware test should become an integration sim test

