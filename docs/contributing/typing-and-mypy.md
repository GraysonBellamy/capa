# Typing and mypy

> **Status:** stub — content to be written.

**Audience:** contributors.
**Scope:** mypy --strict policy, per-module overrides, when to add a typing escape hatch.

## Will cover

- ``mypy.strict = true`` is the baseline
- Per-module overrides for UI deps without ``py.typed``
- Where to add new overrides vs fixing the type
- Pydantic plugin

