# Plugin lockfile

> **Status:** stub — content to be written.

**Audience:** plugin authors and operators.
**Scope:** the per-experiment ``example_plugins.lock`` file: format, generation, verification.

## Will cover

- Why we lock plugins per-experiment (reproducibility)
- Lockfile format
- ``capa plugins lock`` generation
- ``capa plugins verify`` on run-arm
- Updating after a plugin upgrade

