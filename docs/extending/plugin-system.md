# Plugin system

> **Status:** stub — content to be written.

**Audience:** plugin authors and integrators.
**Scope:** how capa discovers plugins via Python entry points and what registries each plugin kind populates.

## Will cover

- Entry-point groups: ``capa.procedures``, ``capa.profiles``, future ``capa.step_kinds``, ``capa.binding_kinds``, ``capa.sinks``
- Trusted registry pattern
- Per-experiment plugin lockfile and why it exists
- ``capa plugins`` CLI

*See also:* [Plugin lockfile](plugin-lockfile.md).

