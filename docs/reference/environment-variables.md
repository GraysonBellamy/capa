# Environment variables

> **Status:** stub — content to be written.

**Audience:** operators, CI, deployment.
**Scope:** every env var capa reads, with default and effect.

## Will cover

- ``CAPA_CONFIGS`` — configs root
- ``CAPA_RUNS_ROOT`` — bundles root
- ``CAPA_HARDWARE_TESTS`` — opt-in hardware test runs
- Logging vars (``CAPA_LOG_LEVEL``, ``CAPA_LOG_JSON``)
- FLIR Atlas SDK paths

