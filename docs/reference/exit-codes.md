# Exit codes

> **Status:** stub — content to be written.

**Audience:** CI authors and scripters.
**Scope:** exit code table for ``capa`` CLI commands.

## Will cover

- 0: success / sealed
- non-zero: see per-command tables
- How outcome (``sealed`` / ``failed`` / ``crashed_but_sealed``) maps to exit codes
- Reserved codes for SIGINT / SIGTERM

